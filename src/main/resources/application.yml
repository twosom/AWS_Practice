
spring:
  datasource:
    username: sa
    password:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/awsPrac
  jpa:
    hibernate:
      ddl-auto: none
    #    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 242989834290-ii9upi5m3nq06bbq70icck2uhh2p1nas.apps.googleusercontent.com
            client-secret: P3FW60vro1Z-jhp52TmzhKbh
            scope: profile, email

          naver:
            client-id: q4gw5KOlD8gaOocHh04Q
            client-secret: UaeySJ74jc
            redirect-uri: '{baseUrl}/{action}/oauth2/code/{registrationId}'
            authorization_grant_type: authorization_code
            scope: name, email, profile_image
            client-name: Naver
        provider:
          naver:
            authorization_uri: https://nid.naver.com/oauth2.0/authorize
            token_uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user_name_attribute: response
  profiles:
    include: oauth
  h2:
    console:
      enabled: true
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: embedded
#        dialect: org.hibernate.dialect.MySQL5Dialect


logging.level:
  org.hibernate.SQL: debug


